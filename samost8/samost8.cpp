#include <iostream>
#include <iomanip>
#include <math.h>
#include <windows.h>
using namespace std;

int main() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    double x, a, c, f;

    cout << "¬вед≥ть a: ";
    }
    else {
        f = x / c;
    }

    cout << "F = " << f << endl;
    system("pause");
    return 0;
}
